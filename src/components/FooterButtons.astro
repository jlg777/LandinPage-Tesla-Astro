---
const { LeftButtons, RightButtons, SingleButtons, title } = Astro.props
---

{
  LeftButtons ? (
    <div class="flex gap-4">
      <a class="bg-blue-500 px-22 py-3 rounded text-xs">Ordenar</a>
      <a class="bg-white text-gray-900 px-20 py-3 rounded text-xs">Más información</a>
    </div>
  ) : RightButtons ? (
    <div class="flex gap-4">
      <a class="bg-white text-gray-900 px-20 py-3 rounded text-xs">Ordenar</a>
      <a class="bg-blue-500 px-22 py-3 rounded text-xs">Más información</a>
    </div>
  ) : SingleButtons ? (
    <div class="flex gap-4">
      <a class="bg-blue-500 px-22 py-3 rounded text-xs">{title}</a>
    </div>
  ) : null
}
